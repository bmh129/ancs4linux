[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ancs4linux"
version = "0.1.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["ancs4linux*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
